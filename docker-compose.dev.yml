version: '3.4'

services:
  curriculum-designer:
    hostname: curriculum_designer
    container_name: curriculum_designer
    restart: always
    build:
      context: "."
    ports:
      - 6060:6060
